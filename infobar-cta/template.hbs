<div id="evg-infobar-cta" style="background-color: {{backgroundColor.hex}}; font-family: {{font}}">
    <div class="evg-container">
        <p class="evg-main" style="color: {{mainTextColor.hex}}">{{{mainText}}}</p>
        <a class="evg-cta" href="{{ctaURL}}" style="color: {{ctaTextColor.hex}}">
            {{{ctaText}}}
        </a>
    </div>
</div>